import styled from 'styled-components'

import Layout from '@/components/Layout'

const NavbarLayout = styled(Layout)`
  margin-top: ${p => p.theme.sizes.navbarHeight}rem;
`

export default NavbarLayout
